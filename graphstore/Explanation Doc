Explanation Doc

tempG.pkl can be read with networkx.readwrite.gpickle.read_gpickle()
	this is a graph with 100 nodes, 5 clusters

dropoffnodes.pkl is an array of drop off points, i.e. a list of drop off nodes
this can be read as:

gi
